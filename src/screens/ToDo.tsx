import React, { useState } from 'react';
import {
  SafeAreaView,
  View,
  Text,
  ScrollView,
  TouchableOpacity,
  TouchableWithoutFeedback,
} from 'react-native';
import { CategoryDetail, GoalCategoryDetail } from '@/types';
import { useTodoData } from 'hooks/todoDataHooks'; // Import the custom hook
import styles from '../css/CalendarTodoStyles';
import CalendarNave from 'Calendar/CalendarNav';
import Calendar from 'Calendar/Calendar';
import CategoryTodos from 'components/CategoryTodos';
import AddTodo from 'components/AddTodo';

const ToDo: React.FC = () => {
 

  return (
    <>
      
    </>
  );
};

export default ToDo;
